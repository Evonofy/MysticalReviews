---
import "swiper/css";
import "swiper/css/pagination";

import Layout from "@/components/layout/layout.astro";
import { Home } from "@/components/layout/pages/home";

import { getCards } from "@/data";

const cards = await getCards();
---

{Boolean(import.meta.env.PUBLIC_DATABASE_ID) && "tá aqui"}
{Boolean(import.meta.env.PUBLIC_NOTION_SECRET) && "tá aqui"}

<Layout title="Home">
  <Home cards={cards} client:only="react" />
</Layout>
